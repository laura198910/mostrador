<%= simple_form_for(@presupuesto) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :presupuesto_id %>
    <%= f.input :fecha %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

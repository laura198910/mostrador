<%= simple_form_for(@categorium) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nombre %>
    <%= f.input :detalle %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
